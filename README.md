# MarketLab Arena

Добро пожаловать в **MarketLab Arena** — пространство для разработки, тестирования и сравнительного анализа торговых стратегий.

Каждая стратегия проходит единый цикл:

**Backtesting → оценка метрик → позиция в лидерборде → запуск в песочнице платформы *Т-Инвестиции***

<!-- LEADERBOARD:START -->
## Актуальный Лидерборд

Автоматически обновляется после каждого бэктеста. Последнее обновление: `20260220T164020Z` UTC.

| Место | Участник | CAGR % | Макс. просадка % | Сделки | EMA Fast | EMA Slow | BB Window | BB Dev | ТФ (мин) |
|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | dmitrii | 39.50 | -11.83 | 81 | 30 | 40 | 20 | 1.0 | 60 |
| 2 | dstarky | 20.87 | -23.71 | 90 | 30 | 35 | 15 | 1.0 | 60 |
| 3 | julia | 14.35 | -19.47 | 22 | 25 | 45 | 20 | 1.0 | 240 |
| 4 | viktor | 13.08 | -16.75 | 26 | 25 | 35 | 15 | 1.0 | 240 |
| 5 | mary | 7.26 | -18.83 | 21 | 25 | 60 | 20 | 1.0 | 240 |
| 6 | gav | 6.94 | -19.43 | 22 | 20 | 50 | 20 | 1.0 | 240 |

<!-- LEADERBOARD:END -->

Проект работает через API **T-Invest (T-Банк Инвестиции)**.

Для запуска потребуется:

1. Создать токен Sandbox в личном кабинете T-Invest.
2. Скопировать токен в формате:

```
t.xxxxxxxxxxxxxxxxx
```

## 🚀 Начало работы

### 1. Клонирование репозитория

```powershell
git clone https://github.com/DmitriiOrel/marketlab-arena.git
cd .\marketlab-arena
```

### 2. Настройка окружения и подключение к платформе *"Т-Инвестиции"*

```powershell
.\setup.ps1 -Token "t.ВАШ_TINVEST_TOKEN"
```

Скрипт:
- создаёт виртуальное окружение,
- устанавливает зависимости,
- формирует `.env` с API-токеном,
- проверяет подключение к песочнице Т-Invest.

---

## ▶ Запуск стратегии

```powershell
$env:GITHUB_TOKEN="github_pat_ВАШ_GITHUB_PAT"
.\run.ps1 20 50 20 2.0 60 -Name ivan
```

### Формат параметров

```
ema_fast ema_slow bb_window bb_dev timeframe_min -Name имя
```

Пример:

```
20 50 20 2.0 60
```

Означает:

- **EMA Fast:** *Быстрая экспоненциальная скользящая средняя (EMA fast) за 20 часов*  
- **EMA Slow:** *Медленная экспоненциальная скользящая средняя (EMA slow) за 50 часов*  
- **Bollinger Window:** *Окно расчёта полос Боллинджера в 20 часов*
- **Bollinger Dev:** *ширина полос Боллинджера* = 2.0 стандартных отклонения  
- **Таймфрейм стратегии:** 60 минут  

---

## ⚙ Логика выполнения

После запуска `run.ps1` система:

1. Подключается к T-Invest API.
2. Загружает исторические данные за 3 года.
3. Проводит бэктест стратегии.
4. Рассчитывает ключевые показатели (CAGR, максимальная просадка, число сделок).
5. Обновляет лидерборд (сохраняется только лучший результат участника).
6. Обновляет таблицу в `README.md`.
7. Сохраняет артефакты запуска.
8. Запускает стратегию в T-Invest Sandbox.

Каждый запуск фиксируется и может быть воспроизведён.

---

### Описание файлов

- `leaderboard.json` — данные общего рейтинга  
- `runs_index.json` — индекс запусков участника  
- `metrics.json` — параметры и рассчитанные показатели стратегии  
- `equity_curve.png` — кривая капитала  
- `trades.csv` — журнал сделок  

---

## 🧠 Принципы ранжирования

- В рейтинге учитывается только лучший результат каждого участника.
- При повторном запуске:
  - если новый результат лучше — он заменяет предыдущий;
  - если хуже — предыдущий лучший сохраняется.
- Позиции в таблице пересчитываются автоматически.

---

### Артефакты участника

Для каждого участника формируется папка:
- `reports/<user>/trials_index.json` - индекс запусков, отсортированный по доходности;
- `reports/<user>/trial_<run_id>/summary.json` - параметры и метрики стратегии;
- `reports/<user>/trial_<run_id>/backtest.png` - график бэктеста;
- `reports/<user>/trial_<run_id>/trades.csv` - журнал сделок.
